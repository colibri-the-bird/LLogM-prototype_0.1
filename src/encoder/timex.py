"""Temporal expression normalization adapter for HeidelTime."""

from __future__ import annotations

from typing import Iterable, List


def normalize_temporal_expressions(sentences: Iterable[str], url: str) -> List[dict]:
    """Call the HeidelTime HTTP wrapper and parse the temporal annotations."""

    raise NotImplementedError
